
function setup() {
    let canvas = createCanvas(560, 315);
    canvas.parent('canvas');
    background(0, 255, 0, 10);
}

function draw() {
    background(0,0.1);
    // circle(mouseX, mouseY, 10);
}